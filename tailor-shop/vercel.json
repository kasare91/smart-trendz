{
  "buildCommand": "prisma generate && next build",
  "installCommand": "yarn install",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/notifications/cron",
      "schedule": "0 9 * * *"
    }
  ]
}
